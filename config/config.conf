data_path = {
    "transactions" = "data/transactions",
    "proofs" = "data/proofs",
    "validated" = "data/validated"
}

llm = {
    "default_provider" = "gemini_flash_lite",
    "primary_model" = "gemini-2.5-flash-lite",
    "fallback_model" = "gemini-2.5-flash-lite"
}

categorize = {
    "enabled" = true,
    "chunk_size" = 100
}

matching = {
    # 0.0..1.0 sequence-similarity threshold used for fuzzy category matching.
    "category_fuzzy_min_ratio" = 0.72
}
